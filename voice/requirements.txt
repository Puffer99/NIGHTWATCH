# NIGHTWATCH Voice Pipeline Dependencies
# Version pinned for reproducible builds (updated January 2026)

# Speech-to-Text (choose one)
faster-whisper~=1.0.3    # Recommended: faster inference with CTranslate2
# openai-whisper~=20231117  # Alternative: official OpenAI package

# Text-to-Speech
piper-tts~=1.2.0         # Fast local TTS

# Audio I/O
sounddevice~=0.5.1
numpy~=1.26

# Voice Activity Detection (see docs/INTEGRATION_PLAN.md Phase 1.3)
pymicro-vad~=1.0.0       # Neural VAD for robust speech detection

# Audio processing (for VAD and preprocessing)
webrtcvad~=2.0.10        # Google WebRTC VAD (fallback)

# Optional: Alternative TTS (uncomment if needed)
# TTS~=0.22.0             # Coqui TTS (larger, more voices)
